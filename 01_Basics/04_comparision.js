console.log(null>0);
console.log(null==0);
console.log(null>=0);

console.log(undefined==0);
console.log(undefined>=0);

//avoid these kinds of conversions -- write clean code
//=== checks both the data type and the data but == only compares the data not the datatype